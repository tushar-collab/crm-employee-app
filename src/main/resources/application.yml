spring:
  application:
    name: crm-employee-app
  datasource:
    url: jdbc:mysql://localhost:3306/crm
    username: root
    password: root
  jpa:
    show-sql: false
  sql:
    init:
      mode: always
logging:
  level:
    root: INFO
  file:
    name: logs/crm-employee-app.log
server:
  port: 8081
  
config:
  maxReviewsCount: 3
